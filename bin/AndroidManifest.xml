<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gradproject_android"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="14" />
	<uses-permission android:name="android.permission.CAMERA" />
	        <uses-permission android:name="android.permission.INTERNET"/>
	
	<uses-feature android:name="android.hardware.camera" />
	<uses-feature android:name="android.hardware.camera.autofocus" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/burger_btn"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.gradproject_android.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
         <activity
            android:name="com.example.gradproject_android.DashboardActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.example.gradproject_android.MainMenuList"
            android:label="@string/app_name" >
        </activity>
        <!-- <activity
            android:name="com.example.gradproject_android.MainActivity"
            android:label="@string/app_name" >
        </activity> -->
		 <!--  Individual Activity -->
        <activity
            android:label="Gift List" 
            android:name="com.example.gradproject_android.IndividualList"></activity>

        
        <!--  Login Activity -->
        <activity
            android:label="Login Account" 
            android:name="com.example.gradproject_android.LoginActivity"></activity>

        <!--  Register Activity -->
        <activity
            android:label="Register New Account" 
            android:name="com.example.gradproject_android.RegisterActivity"></activity>
    </application>

</manifest>
